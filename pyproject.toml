[project]
name = "adbc-driver-oracle"
description = "An Apache Arrow ADBC driver for Oracle Database — pure Go, no Oracle Client libraries required."
authors = [{ name = "GizmoData", email = "support@gizmodata.com" }]
license = "MIT"
license-files = ["LICENSE", "NOTICE"]
readme = "README.md"
requires-python = ">=3.10"
dynamic = ["version"]
keywords = [
    "adbc",
    "adbc-driver",
    "apache-arrow",
    "arrow",
    "database",
    "gizmodata",
    "oracle",
    "pyarrow",
    "sql",
]
dependencies = [
    # >=1.11.0 for driver-manifest + connection-profile resolution
    # (profile=..., uri="profile://...", oracle:// scheme lookup).
    "adbc-driver-manager >=1.11.0",
    "importlib-resources >=1.3",
    # pyarrow is required for any DBAPI cursor method that returns
    # Arrow data (fetch_arrow_table, fetch_record_batch, etc.) — i.e.
    # every quickstart example in the README. Real dep, not optional.
    "pyarrow >=14.0.1",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows",
    "Topic :: Database",
    "Topic :: Database :: Database Engines/Servers",
]

[project.optional-dependencies]
dbapi = ["pandas"]
test = ["pandas", "pytest >=7", "duckdb >=1.3", "shapely >=2.0"]
# Oracle -> GizmoSQL streaming-ingest integration test (the customer-notebook flow).
gizmosql = ["adbc-driver-gizmosql >=2.0"]

[project.urls]
homepage = "https://github.com/gizmodata/adbc-driver-oracle"
repository = "https://github.com/gizmodata/adbc-driver-oracle"
issues = "https://github.com/gizmodata/adbc-driver-oracle/issues"

[build-system]
requires = ["setuptools >= 77.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
include-package-data = true
# Python sources live under python/ so the repo can be polyglot (Go at
# the root, Python here, cgo wrapper in pkg/oracle/). Be explicit about
# what we package — auto-find would also sweep up python/tests/.
packages = ["adbc_driver_oracle"]
package-dir = { "" = "python" }

[tool.pytest.ini_options]
xfail_strict = true
testpaths = ["python/tests"]
markers = [
    "integration: requires a live Oracle Database (ADBC_ORACLE_TEST_URI; see python/tests/conftest.py)",
    "gizmosql: additionally requires a live GizmoSQL (GIZMOSQL_URI / GIZMOSQL_TOKEN)",
    "duckdb_ext: requires the duckdb package and the adbc_scanner community extension",
]
